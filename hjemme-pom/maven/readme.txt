Maven 2 configuration.

server.xml	The configuration of the local maven repository.
    A proxy setting for using this setting on other sites is also available (commented).
checkstyle.xml	The checkstyle xml which is put to use in the checkstyle plugin. Configured to
    http://www.hjemme.nu/checkstyle.xml in the pom.